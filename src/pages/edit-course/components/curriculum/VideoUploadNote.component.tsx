const VideoUploadNote = () => {
  return (
    <span className="note">
      <b>Note: </b>
      Video phải đạt tối thiểu 720p và không vượt quá 1.0 GB.
    </span>
  );
};

export default VideoUploadNote;
